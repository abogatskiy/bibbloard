# Secrets and tokens
.env
.mcp.json

# Deployment scripts and instructions (contain infra details, stay local)
.claude/
CLAUDE.md
DEPLOYMENT.md
deploy.sh

# Raw data — tracked in repo for preservation (CSVs scraped, all.json downloaded)
# all.json (~42MB) and CSVs (~38MB) are safe under GitHub's 100MB file limit


# Generated chart data — produced by bibbloard.py from raw/ sources, not committed
data/*.json
# Except the files needed for the news chyron
!data/prev_snapshot.json
!data/latest_news.json
output/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
